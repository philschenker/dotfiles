# select window (tab) using ctrl+page-up/down
bind -n C-pagedown next-window
bind -n C-pageup previous-window
bind -n C-t new-window

set-option -g mouse on

# Load theme
source ~/.config/tmux/airline-solarized-insert.tmuxtheme
